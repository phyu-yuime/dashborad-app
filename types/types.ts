export interface FormData{
    email: string;
    password: string;
}
    
export interface Memo {
    id: number;
    title: string;
    content: string;
}


export interface User {
    id: number;
    username: string;
    email: string;
}
